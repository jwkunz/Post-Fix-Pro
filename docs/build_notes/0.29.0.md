# Build Notes - v0.29.0

## Summary
- Bumped crate minor version from `0.28.0` to `0.29.0`.
- Added scalar scientific-notation entry support improvements:
  - Scalar `EXP` button now inserts `E` into the entry buffer.
  - Keyboard `e` / `E` now inserts exponent marker for scalar entry.
  - Scalar entry parser now validates incremental scientific notation forms.
  - Exponent sign entry (`+`/`-`) is supported after `E`.
- Confirmed matrix and complex parsing supports `E` notation via parse-on-submit behavior.
- Updated `help.md` to document `E`/`EXP` usage and notation examples.

## Commands Run
- `./scripts/package_dist_folder.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/index.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
