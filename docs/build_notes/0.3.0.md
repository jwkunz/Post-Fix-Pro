# Build Notes - v0.3.0

## Summary
- Bumped crate minor version from `0.2.0` to `0.3.0`.
- Added core Rust backend scaffold in `src/lib.rs`.
- Added unit tests for entry/stack operations and state-preserving error behavior.
- Ran test suite and release build.
- Copied `index.html` to `dist/index.html`.

## Commands Run
- `cargo test`
- `cargo build --release`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`7` unit tests in `src/lib.rs`)
- Release build: success

## Backend Scope Added
- Domain types:
  - `Value` (`Real`, `Complex`, `Matrix`)
  - `Complex`
  - `Matrix` with dimension validation
  - `CalcError`
  - `CalcState`
  - `Calculator`
- Core operations:
  - `enter`
  - `drop`
  - `dup`
  - `swap`
  - `rot`
  - `clear_entry`
  - `clear_all`
