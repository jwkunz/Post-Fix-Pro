# Build Notes - v0.16.0

## Summary
- Bumped crate minor version from `0.15.0` to `0.16.0`.
- Updated stack rendering so older values remain higher and the top-of-stack is shown at the bottom.
- Added top-of-stack marker `T` in the stack list.
- Ran tests and release build.
- Copied `index.html` to `dist/index.html`.

## Commands Run
- `cargo test`
- `cargo build --release`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`41` tests total)
- Release build: success
