# Build Notes - v2.15.0

## Summary
- Bumped crate version from `2.14.0` to `2.15.0`.
- Continued roadmap by lifting the SVD real-only limitation.
- Backend SVD now supports complex matrices directly using complex `nalgebra` decomposition.
- Removed the previous conversion path that forced real-only SVD inputs.
- Updated documentation to reflect complex-capable SVD behavior.
- Added/updated regression coverage:
  - backend SVD reconstruction test now includes a complex matrix case
  - API SVD test now includes a complex matrix input case

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/package_dist_folder.sh`

## Build Result
- Tests: `69 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
