# Build Notes - v2.1.0

## Summary
- Bumped crate version from `2.0.0` to `2.1.0`.
- Added vector statistics operations (vector matrices `Nx1` / `1xN`) in backend:
  - `mean`
  - `mode`
  - `variance` (population)
  - `std_dev` (population)
  - `max`
  - `min`
- Statistics operations currently require real-valued vectors.
- Wired new operations through:
  - `CalculatorApi`
  - wasm bindings
  - UI action map
- Added a dedicated **Statistics** panel in `Post_Fix_Pro.html` with buttons:
  - `mean`, `mode`, `std dev`, `variance`, `max`, `min`
- Updated `help.md` with matrix op additions (`trace`, `dot`, `cross`, `norm_p`) and the new statistics section.
- Added backend and API tests for all new statistics operations.

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/package_dist_folder.sh`

## Build Result
- Tests: `53 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
