# Build Notes - v0.30.0

## Summary
- Bumped crate minor version from `0.29.0` to `0.30.0`.
- Added new complex entry mode:
  - `a * exp(i 2 pi b)`
- Updated complex conversion logic:
  - For this mode, compute `theta = 2πb`, then `re = a*cos(theta)`, `im = a*sin(theta)`.
- Updated complex entry UI text and accessibility labels for the new mode.
- Updated `help.md` documentation for the new complex entry mode.

## Commands Run
- `./scripts/build_wasm.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/index.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
