# Build Notes 3.3.0

## Summary

- Rebuilt `help.md` from scratch to match the current GUI panel layout and button naming.
- Added explicit stack argument-order documentation for multi-argument operations.
- Fixed tooltip parsing coverage by ensuring all mapped tooltip keys exist in `help.md`.
- Resolved tooltip key gaps for `decimal` and `norm-p`.

## Validation

- `cargo test`
- `./scripts/package_dist_folder.sh`

