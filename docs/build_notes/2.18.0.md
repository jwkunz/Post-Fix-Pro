# Build Notes - v2.18.0

## Summary
- Bumped crate version from `2.17.0` to `2.18.0`.
- Fixed a frontend runtime regression that could make the app appear unresponsive after successful operations:
  - Restored missing `applyDecompositionLabels` and `clearStackRoleLabels` functions in `Post_Fix_Pro.html`.
  - Reconnected decomposition role rendering (`Q/R`, `P/L/U`, `U/S/Vt`, `V/D`) in the stack display.
  - Improved runtime missing-method error text to include the method name.
- Refreshed `dist/Post_Fix_Pro.html` through the wasm build flow.

## Commands Run
- `cargo test`
- `./scripts/package_dist_folder.sh`

## Build Result
- Tests: `69 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/Post_Fix_Pro.png`, `dist/pkg`, `dist/wasm_base64.js`).
