# Build Notes - v0.11.0

## Summary
- Bumped crate minor version from `0.10.0` to `0.11.0`.
- Exposed stack utility operations through Rust API and WASM layer:
  - `drop`
  - `dup`
  - `swap`
  - `rot`
- Added stack utility controls to the browser keypad and wired them through the thin JS bridge.
- Added a keypad shortcut for identity push (`I(n)`) using the existing identity size input.
- Expanded API tests for stack utility behavior through the structured response interface.
- Ran tests and release build.
- Copied `index.html` to `dist/index.html`.

## Commands Run
- `cargo test`
- `cargo build --release`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`32` tests total)
- Release build: success

## Scope Added
- Rust API (`src/api.rs`):
  - `CalculatorApi::drop`
  - `CalculatorApi::dup`
  - `CalculatorApi::swap`
  - `CalculatorApi::rot`
  - wasm exports:
    - `WasmCalculator::drop`
    - `WasmCalculator::dup`
    - `WasmCalculator::swap`
    - `WasmCalculator::rot`
- UI (`index.html`):
  - Keypad buttons and action mapping for `drop/dup/swap/rot`
  - Keypad action for `push-identity` wired to identity input
