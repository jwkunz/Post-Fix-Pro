# Build Notes - v0.7.0

## Summary
- Bumped crate minor version from `0.6.0` to `0.7.0`.
- Added a thin API layer (`src/api.rs`) with structured `{ ok, state, error }` responses.
- Added serializable state/value DTOs for JS/WASM boundary exchange.
- Added wasm-target wrapper (`WasmCalculator`) returning JSON responses.
- Expanded test suite with API response and snapshot coverage.
- Ran tests and release build.
- Copied `index.html` to `dist/index.html`.

## Commands Run
- `cargo test`
- `cargo build --release`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`28` tests total)
- Release build: success

## Backend Scope Added
- New module:
  - `src/api.rs`
- New API-facing types:
  - `ApiValue`
  - `ApiAngleMode`
  - `ApiDisplayMode`
  - `ApiState`
  - `ApiError`
  - `ApiResponse`
- New API wrapper:
  - `CalculatorApi` with methods that return structured responses and snapshots
- New wasm shim (wasm32-only):
  - `WasmCalculator` methods returning serialized JSON payloads
- New dependencies:
  - `serde` (derive)
  - `serde_json`
  - `wasm-bindgen`
