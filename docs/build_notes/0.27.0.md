# Build Notes - v0.27.0

## Summary
- Bumped crate minor version from `0.26.0` to `0.27.0`.
- Improved complex number entry UX to allow unrestricted typing before parse:
  - Switched complex entry fields from `type=\"number\"` to `type=\"text\"`.
  - Parsing/validation now occurs only when pressing the complex push button.
  - Added explicit parse errors for empty/invalid field formats.
- Updated help docs to clarify parse-on-submit behavior for matrix and complex inputs.

## Commands Run
- `./scripts/package_dist_folder.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/index.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
