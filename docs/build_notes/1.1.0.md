# Build Notes - v1.1.0

## Summary
- Bumped crate version from `1.0.0` to `1.1.0`.
- Added MIT licensing:
  - Added `LICENSE` with MIT text.
  - Added `license = "MIT"` in `Cargo.toml`.
  - Updated `README.md` license section.
- Added scalar touch/click edit mode:
  - Tapping/clicking scalar entry opens inline text edit.
  - Supports manual typed input and validates scalar format before committing.
  - `Enter` commits, `Escape` cancels, blur commits.
- Renamed UI file from `index.html` to `Post_Fix_Pro.html` and updated active docs/scripts/messages.
- Updated build flow to ship `dist/Post_Fix_Pro.html` and remove legacy `dist/index.html`.
- Updated release workflow asset naming to:
  - `Post_Fix_Pro_vX.Y.Z.zip`

## Commands Run
- `./scripts/package_dist_folder.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
