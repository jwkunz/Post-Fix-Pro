# Build Notes - v3.1.1

## Summary
- Bumped crate version from `3.1.0` to `3.1.1`.
- Added keyboard hotkey for undo:
  - `u` / `U` now triggers `undo`.
- Updated help documentation keyboard shortcuts to include undo hotkey.

## Commands Run
- `cargo test`
- `./scripts/package_dist_folder.sh`

## Build Result
- Tests: `83 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
