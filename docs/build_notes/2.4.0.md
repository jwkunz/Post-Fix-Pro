# Build Notes - v2.4.0

## Summary
- Bumped crate version from `2.3.0` to `2.4.0`.
- Added `SVD` operation (singular value decomposition):
  - Backend computes `U`, `S`, `Vt` for real-valued matrices.
  - Stack behavior: replaces top matrix with `U`, then pushes `S`, then `Vt`.
- Added real-matrix conversion helpers for decomposition pipeline.
- Wired `svd` through:
  - Calculator API
  - wasm bindings
  - Matrix panel UI button and action mapping
- Updated `help.md` with SVD behavior and stack output convention.
- Added tests:
  - Backend reconstruction test (`U * S * Vt` matches input).
  - API coverage for `svd` shape and stack behavior.

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `59 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
