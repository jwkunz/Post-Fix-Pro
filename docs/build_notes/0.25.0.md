# Build Notes - v0.25.0

## Summary
- Bumped crate minor version from `0.24.0` to `0.25.0`.
- Added matrix size controls to the Matrix panel:
  - `Size A` (rows, cols, `Apply`)
  - `Size B` (rows, cols, `Apply`)
- Clicking `Apply` now regenerates matrix textareas with the selected dimensions using zero-filled templates.
- Preset buttons now also sync the size control values with preset dimensions.
- Updated `help.md` to document matrix size controls.

## Commands Run
- `./scripts/build_wasm.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/index.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
