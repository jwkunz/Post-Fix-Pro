# Build Notes - v0.15.0

## Summary
- Bumped crate minor version from `0.14.0` to `0.15.0`.
- Wrapped up outstanding backend test coverage for advanced scientific and memory features.
- Verified release and wasm distribution flows after test additions.

## Commands Run
- `cargo test`
- `cargo build --release`
- `./scripts/package_dist_folder.sh`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`41` tests total)
- Release build: success
- WASM distribution build: success

## Coverage Additions
- Added tests for:
  - `pow` and `%`
  - inverse trig behavior in degree mode (`asin`)
  - hyperbolic functions (`sinh`, `cosh`)
  - `log10`
  - `gamma` and `erf`
  - constants (`pi`, `e`)
  - memory register flows (`store`, `recall`, `clear`)
  - memory error handling (`invalid register`, `empty register`)
