# Build Notes - v3.0.0

## Summary
- Bumped crate version from `2.30.3` to `3.0.0`.
- Updated calculator GUI button captions from math-friendly notation to Rust/API-style names.
- Limited changes to label text only (no command wiring or backend behavior changes).
- Refreshed distributable HTML with the new labeling.

## Revertability
- This release is intentionally a single isolated UI-label commit so it can be reverted in one step if the naming layout is not preferred.

## Commands Run
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `82 passed, 0 failed`.
- WASM release build: success.
- Dist artifact refreshed: `dist/Post_Fix_Pro.html`.
