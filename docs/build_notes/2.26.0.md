# Build Notes - v2.26.0

## Summary
- Bumped crate version from `2.25.0` to `2.26.0`.
- Added stack visibility control to the stack panel UI:
  - New integer spinner (`1..64`) labeled `Visible`.
  - Controls how many top stack frames are rendered.
  - Hidden stack values remain in backend state and continue participating in operations.
- Updated stack rendering behavior:
  - Shows only the configured number of top frames.
  - Preserves true stack indices (`#n`) and top marker (`T`).
  - Shows a compact hidden-count row when older frames are not displayed.
- Updated help documentation to describe the new stack visibility control.

## Commands Run
- `cargo test`
- `./scripts/package_dist_folder.sh`

## Build Result
- Tests: `82 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/Post_Fix_Pro.png`, `dist/pkg`, `dist/wasm_base64.js`).
