# Build Notes - v0.31.0

## Summary
- Bumped crate minor version from `0.30.0` to `0.31.0`.
- Added requested physical constants to the `Constants` panel:
  - Euler-Mascheroni constant (`gamma`)
  - Speed of light (`c`)
  - Avogadro constant (`mol`)
  - Boltzmann constant (`k`)
  - Reduced Planck constant (`hbar`)
  - Vacuum permittivity (`epsilon_0`)
  - Vacuum permeability (`mu_0`)
  - Newtonian gravitational constant (`G`)
- Implemented constant button handling in UI action dispatch:
  - Each constant button now pushes the corresponding numeric value onto the stack.
- Updated `help.md` to list constant values and labels.

## Commands Run
- `./scripts/package_dist_folder.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/index.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
