# Build Notes - v3.0.3

## Summary
- Bumped crate version from `3.0.2` to `3.0.3`.
- Added delayed hover tooltips for all calculator buttons:
  - Tooltip appears after `2` seconds of mouse hover.
  - Tooltip text is loaded from `help.md` at runtime.
  - Button actions/IDs are mapped to help entry keys to keep tooltip text in sync with documentation.
- Added tooltip UI styling and placement logic in `Post_Fix_Pro.html`.
- Added help entries for dialog controls:
  - `help`
  - `close`

## Commands Run
- `cargo test`
- `./scripts/package_dist_folder.sh`

## Build Result
- Tests: `82 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
