# Build Notes - v2.12.0

## Summary
- Bumped crate version from `2.11.0` to `2.12.0`.
- Added golden ratio constant in constants panel:
  - label: `ψ`
  - value: `1.618033988749895`
- Added complex panel operators:
  - `real()` extracts real part of top scalar
  - `imag()` extracts imaginary part of top scalar
- Wired `real()` and `imag()` through:
  - backend calculator core (`src/lib.rs`)
  - API layer + wasm bindings (`src/api.rs`)
  - frontend action map (`Post_Fix_Pro.html`)
- Updated help documentation for new complex operators and `ψ` constant.
- Added regression tests:
  - backend coverage for `real_part` / `imag_part`
  - API coverage for `real_part` / `imag_part`

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `69 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
