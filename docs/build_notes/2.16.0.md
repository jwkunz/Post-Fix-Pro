# Build Notes - v2.16.0

## Summary
- Bumped crate version from `2.15.0` to `2.16.0`.
- Added decomposition result labeling in stack UI for faster interpretation:
  - `QR` labels top results as `Q`, `R`
  - `LU` labels top results as `P`, `L`, `U`
  - `SVD` labels top results as `U`, `S`, `Vt`
  - `EVD` labels top results as `V`, `D`
- Labels are displayed as small role badges next to stack indices.
- Labels automatically clear on subsequent successful non-decomposition operations.
- Updated help docs to document decomposition stack tags.

## Commands Run
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `69 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
