# Build Notes - v2.30.0

## Summary
- Bumped crate version from `2.29.0` to `2.30.0`.
- Refactored test organization to improve maintainability:
  - Moved `src/lib.rs` unit tests into `src/tests/lib_tests.rs`.
  - Moved `src/api.rs` unit tests into `src/tests/api_tests.rs`.
  - Kept tests wired as unit tests via `#[path = "tests/..."] mod tests;` in each source file.
- Reduced file size and noise in `src/lib.rs` and `src/api.rs` without changing runtime behavior.

## Commands Run
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `82 passed, 0 failed`.
- WASM release build: success.
