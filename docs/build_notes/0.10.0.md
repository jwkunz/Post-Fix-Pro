# Build Notes - v0.10.0

## Summary
- Bumped crate minor version from `0.9.0` to `0.10.0`.
- Added direct complex-number entry through Rust API and WASM bridge.
- Added identity-matrix push through Rust API and WASM bridge.
- Enhanced `index.html` with matrix helper controls:
  - A/B presets
  - Identity size input + push button
- Added complex entry controls in UI:
  - real/imaginary inputs
  - push complex button
- Ran tests and release build.
- Copied `index.html` to `dist/index.html`.

## Commands Run
- `cargo test`
- `cargo build --release`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`31` tests total)
- Release build: success

## Scope Added
- Rust API (`src/api.rs`):
  - `ComplexInput`
  - `CalculatorApi::push_complex`
  - `CalculatorApi::push_identity`
  - wasm exports:
    - `WasmCalculator::push_complex(re, im)`
    - `WasmCalculator::push_identity(size)`
- UI (`index.html`):
  - Matrix preset buttons for quick A/B population
  - Identity push control from size input
  - Complex push controls for direct stack insertion
